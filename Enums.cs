using ExileCore.PoEMemory.Components;
using ExileCore.PoEMemory.MemoryObjects;
using SharpDX;

namespace Follower
{
    public enum FollowerType
    {
        Follower = 100,
        Leader = 200,
        Disabled = 300
    }

    public enum FollowerAggressiveness
    {
        Aggressive = 100,
        Passive = 200,
        Disabled = 300
    }
}
